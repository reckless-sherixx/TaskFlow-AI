global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: "taskflow-app"
    metrics_path: /api/metrics
    static_configs:
      - targets: ["app:3000"]
        labels:
          service: taskflow
